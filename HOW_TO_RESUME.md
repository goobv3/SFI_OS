# 프로젝트 재개 가이드 (2026-03-13 업데이트)

## 1. 현재 진행 상황
- **UI 최적화**: 화면 활용도를 높이기 위해 컴팩트 레이아웃으로 개편되었습니다. 사이드바가 제거되고 헤더 드롭다운을 통해 재배동을 선택합니다.
- **백엔드 기능**: C++ 백엔드(`sf_backend`)에서 센서 설정 저장(임계값 등)이 불가능하던 문제가 해결되었습니다. CRUD 라우트와 CORS 처리가 완료되었습니다.
- **마이그레이션**: Python 백엔드에서 C++ 백엔드로의 핵심 기능 이관이 거의 완료된 상태입니다.

## 2. 주요 확인 사항
- **센서 설정**: 설정 아이콘(톱니바퀴)을 눌러 상/하한값을 변경하고 '저장'을 누르면 DB에 정상적으로 반영됩니다.
- **CORS 이슈**: `crow::App<crow::CORSHandler>`를 사용하여 브라우저 차단 문제를 해결했으므로, 향후 새로운 API 추가 시 `Router.cpp`에 라우트만 등록하면 됩니다.

## 3. 다음 작업 권장 사항
1. **히스토리 차트 최적화**: 현재 데이터 로딩 속도 및 차트 UI의 세부적인 간격 조정이 필요할 수 있습니다.
2. **알람 시스템 연동**: 센서 데이터가 설정한 임계값을 벗어났을 때 실시간으로 대시보드와 알람 목록에 반영되는지 최종 검증이 필요합니다.
3. **C++ 라이브러리 문서화**: 새로 추가된 `HouseManager`의 CRUD 메서드들에 대해 `HOUSE_LIB_MANUAL.md` 등에 업데이트가 필요합니다.

## 4. 실행 방법
- **백엔드**: `docker-compose up -d backend` (C++ 빌드 포함됨)
- **프론트엔드**: `cd frontend; npm run dev`
- **데이터베이스**: MariaDB (sf_mariadb)
