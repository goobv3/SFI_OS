# Smart Farm Intelligence OS - 파일 역할 명세서 📖

이 문서는 프로그래밍 초보자도 스마트팜 프로젝트(Antigravity Project)의 구조와 각 파일의 핵심 역할을 쉽게 이해할 수 있도록 작성되었습니다. 
전체 시스템은 크게 세 부분으로 나뉩니다: **프론트엔드(화면/UI)**, **백엔드(서버/데이터처리)**, **데이터베이스(정보저장)**.

---

## 1. 프로젝트 최상위 폴더 (Root Directory)
프로젝트 전체의 환경 설정과 구동을 담당하는 파일들이 모여 있습니다.

- **`docker-compose.yml`**
  - **역할:** 여러 개의 프로그램(프론트엔드, 백엔드, 데이터베이스)을 한 번의 명령어로 동시에 실행시키기 위한 "지휘자" 역할의 파일입니다. 
  - **비유:** 오케스트라의 지휘자처럼 각각의 서버가 언제, 어떻게 켜지고 서로 연결될지를 지시합니다.
  
- **`README.md`**
  - **역할:** 이 프로젝트가 무엇인지, 어떻게 사용하는지 요약해 놓은 "프로젝트 설명서"이자 "간판"입니다.

---

## 2. 데이터베이스 폴더 (`/database`)
데이터 리스트를 저장하고 관리하는 공간입니다. 예를 들어 온도, 습도, 기기 목록 등을 영구적으로 기억합니다.

- **`init.sql`**
  - **역할:** 데이터베이스가 처음 만들어질 때 뼈대(테이블 구조)를 어떻게 짤지 지시하는 설계도입니다.
  - **비유:** 호텔을 지을 때 '1층은 로비', '2층은 객실'이라고 방의 용도를 미리 정해두는 도면과 같습니다.

---

## 3. 백엔드 폴더 (`/backend`)
사용자의 요청(화면의 버트 클릭)을 받아 데이터를 처리하고 결과를 넘겨주는 "뇌" 역할을 하는 Python 서버입니다.

- **`main.py`**
  - **역할:** 백엔드의 중심이자 가장 중요한 메인 프로그램입니다. 각종 데이터 조회, 기기 조작 요청을 받아 처리하며, 기상청(KMA) 날씨 데이터를 주기적으로 가져오는 일도 합니다. API 주소 체계들이 모두 이곳에 정의되어 있습니다.
- **`database.py`**
  - **역할:** Python 백엔드 코드가 MariaDB(데이터베이스)와 안전하게 연결하고 통신할 수 있게 해주는 "통역기 및 다리" 역할입니다.
- **`arbitration.py`**
  - **역할:** 제어기(예: 환풍기, 창문 등)를 켜고 끌 때 충돌이나 위험이 발생하지 않도록 중재(인터락)하는 "AI 비서 및 안전 관리자"입니다. 예를 들어 수동 모드일 때는 자동 모드가 작동하지 않게 막아줍니다.
- **`Dockerfile` 및 `requirements.txt`**
  - **역할:** 백엔드 서버를 독립된 환경(도커)에서 문제 없이 띄우기 위해 필요한 파이썬 라이브러리 목록과 설치 설명서입니다.

---

## 4. 프론트엔드 폴더 (`/frontend/src`)
사용자 눈에 보이는 예쁜 화면과 버튼들을 그리는 "얼굴" 역할을 하는 React(Typescript) 폴더입니다.

### 🏠 4.1. 메인 및 기초 파일
- **`main.tsx` & `App.tsx`**
  - **역할:** 화면이 처음 켜질 때 가장 먼저 실행되는 대문 파일입니다. 앱의 전체 화면 레이아웃과 전체 디자인 골격을 결정합니다. 화면 분할이나 라우팅(메뉴 이동)을 담당합니다.
- **`index.css` & `App.css`**
  - **역할:** 화면의 전반적인 색상, 애니메이션, 폰트 스타일 등을 꾸며주는 "디자인 스타일 파일"입니다. 네온(Neon) 테마가 이곳에서 정의됩니다.

### 🌐 4.2. API 통신 구역 (`/api`)
- **`client.ts`**
  - **역할:** 프론트엔드 화면이 백엔드 서버에게 데이터(날씨, 센서값 등)를 "부탁(요청)"할 때 사용하는 우체부 겸 연락책 파일입니다.

### 🧩 4.3. UI 컴포넌트 구역 (`/components`)
화면을 구성하는 작은 레고 블록 단위의 부품(컴포넌트) 파일들이 모여있는 핵심 폴더입니다.

- **`WeatherWidget.tsx`**
  - **역할:** 기상청 날씨와 온실 내부의 기상대 날씨를 비교해서 보여주고 예측 날씨를 확인하는 "날씨 위젯" 화면 부품입니다.
- **`SensorDashboard.tsx` & `SensorGauge.tsx`**
  - **역할:** 여러 개의 센서값(온도, 습도 등)을 원형 게이지(도넛 차트) 등 눈에 띄게 보여주는 "센서 계기판"입니다. 임계치(위험 수준)에 따라 색상이 실시간으로 변합니다.
- **`HistoryChart.tsx`**
  - **역할:** 과거의 센서 기록들을 그래프(차트) 형태로 그려서 추이를 분석할 수 있게 해주는 화면 부품입니다.
- **`ActuatorControl.tsx`**
  - **역할:** 창문 열기, 유동팬 켜기 등 실제 기계들을 조작할 수 있는 "컨트롤러 스위치보드"입니다.
- **`AlarmToast.tsx`**
  - **역할:** 온도가 너무 높거나 낮으면 우측 편에 위험 알람(토스트 팝업)을 띄워주는 "경보 알리미"입니다.
- **`ManageMode.tsx` & `SensorConfigModal.tsx` & `HouseSelector.tsx`**
  - **역할:** 온실의 이름이나 센서의 설정을 관리(추가/삭제/순서변경)할 때 켜지는 환경설정화면 관련 부품 모음입니다.

---
*스마트팜 지능형 운영체제(Antigravity OS)는 위와 같이 화면(Frontend) -> 백엔드(서버) -> 데이터베이스(저장소) 형태로 유기적으로 데이터를 주고받으며 작동합니다.*
