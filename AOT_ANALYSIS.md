# AoT (Any of Things) 저장소 분석 기반 스마트팜 최적화 리포트

요청하신 [AoT-inc/AoT](https://github.com/AoT-inc/AoT) 모니터링 서버 시스템의 소스코드를 분석하여, 현재 우리가 구축 중인 스마트팜(`SFI_OS`) 프로젝트에 적용할 수 있는 최적화 방향성을 다음과 같이 도출했습니다.

## 1. AoT 시스템의 핵심 아키텍처 특성 분석
AoT 프로젝트를 분석한 결과, 이 시스템은 중앙 집중형 클라우드 서버라기보다는 **엣지 컴퓨팅(Edge Computing) 노드 장비**(예: 라즈베리 파이)에 직접 설치되어 구동되는데 특화되어 있습니다.
- **다양한 입력단(Input) 구조**: `aot/inputs` 폴더를 보면 `bme280(온습도)`, `ds18b20(수온)`, `mcp3008(아날로그 칩)` 등 센서 칩셋 자체와 I2C/SPI 방식으로 직접 통신하는 파이썬 코드가 수백 개 구현되어 있습니다.
- **모듈화 된 데몬(Daemon)**: `aot_daemon.py`가 중앙 엔진 역할을 하며, 각 센서를 별도의 스레드 워커로 분리해 지속적으로 값을 읽어오는 특징이 있습니다.
- **통신 프로토콜 지원**: 범용적인 연결을 위해 `mqtt_paho.py` 플러그인을 사용하여 외부 브로커의 특정 Topic을 구독(Subscribe)하며 값을 읽어옵니다.

## 2. 우리 프로젝트(SFI_OS)와의 차이점
- 우리가 구축한 **SFI_OS 백엔드**는 현재 FastAPI 기반의 가벼운 **REST API 웹 서버**에 가깝습니다 (`POST /api/sensors`).
- 즉, 라즈베리파이/아두이노 등이 센서값을 스스로 측정한 뒤 **알아서 SFI_OS 서버의 IP 주소로 HTTP POST 요청을 쏴야만** 기록되는 수동적인(Passive) 방식입니다.

## 3. SFI_OS 최적화 및 업그레이드 제안 (도입 권장 사항)

### A. 프로토콜 다변화 (MQTT 브로커 도입 권장)
- **현재 상황**: HTTP POST 방식은 쉽고 빠르지만, 농장의 기기가 수백 개 단위로 늘어나거나 네트워크가 불안정할 경우 연결 성립(Handshake)에 걸리는 오버헤드가 매우 큽니다.
- **최적화 방향**: AoT 코드의 `mqtt_paho.py` 구현체와 같이, **MQTT 프로토콜(예: Mosquitto Broker)**을 도입하는 것을 강력히 권장합니다.
- **장점**: MQTT는 아주 가볍고 실시간 양방향 통신이 되기 때문에, 센서 배터리 소모가 적고, 구동기 제어 명령(Control Open/Close)을 내릴 때에도 즉각적인 1초 미만 반응 속도를 보장할 수 있습니다.

### B. 데이터 구조체 캐싱 및 Ingot 단위 배치 처리 (Data Ingestion)
- **현재 상황**: `sensor_lib.py`는 데이터가 1건 들어올 때마다 매번 `INSERT INTO sensors` 쿼리를 실행합니다.
- **최적화 방향**: 센서 데이터 유입량이 커지면 DB 병목이 생깁니다. 메모리 단위 큐(Queue/Redis)를 두어, 매 초 단위로 들어오는 측정값을 모았다가 1분 혹은 특정 갯수 단위로 묶어서 `INSERT 다중 쿼리`로 일괄 처리(Bulk Insert)하는 캐싱 엔진을 도입하면 서버 부하를 획기적으로 줄일 수 있습니다.

### C. 데몬 프레임워크 기반의 Task Scheduler 세분화
- **현재 상황**: 기상청 날씨를 가져오는 등 주기적인 작업(Background Task)을 FastAPI 내장 쓰레드로 단순하게 돌리고 있습니다.
- **최적화 방향**: AoT의 `aot_daemon.py` 또는 `Celery` 와 같은 독립적인 워커(Worker) 프로세스를 만들어 비동기 작업 통제권을 강화해야 합니다. 이를 통해 "비가 왔을 때 자동으로 알람을 치고 구동기를 닫는" 등의 자동화 시나리오(Automation Condition)를 안전하게 순환시킬 수 있습니다.

## 4. 즉시 적용 가능한 마일스톤 제안
가장 시급하고 실용적인 다음 마일스톤으로 **[MQTT 브로커 도커(Mosquitto) 설치 및 `SensorManager` 내 MQTT 구독 엔진 융합]** 작업을 제안드립니다. 이를 구현하면 아두이노/ESP32 하드웨어 연동 시 HTTP 서버 코드를 작성하는 것보다 훨씬 간편하고 프로페셔널한 네트워크 아키텍처가 완성됩니다.
